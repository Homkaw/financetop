name: Build Kivy Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      ANDROID_SDK_ROOT: /opt/android-sdk

    steps:
      - name: 📥 Клонируем репозиторий
        uses: actions/checkout@v4

      - name: ⚙️ Устанавливаем зависимости
        run: |
          sudo apt update
          sudo apt install -y \
            build-essential \
            git \
            zip \
            unzip \
            openjdk-17-jdk \
            python3-pip \
            python3-setuptools \
            python3-venv \

      - name: 🧰 Установка зависимостей Python (включая distutils)
        run: |
          sudo apt update
          sudo apt install -y python3-distutils


      - name: 🐍 Настраиваем виртуальное окружение и Buildozer
        run: |
          python3 -m venv venv
          source venv/bin/activate
          pip install --upgrade pip
          pip install buildozer cython

      - name: 📦 Устанавливаем Android SDK cmdline-tools
        run: |
          mkdir -p $ANDROID_SDK_ROOT/cmdline-tools
          cd $ANDROID_SDK_ROOT/cmdline-tools
          curl -sSLo tools.zip https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
          unzip -q tools.zip -d temp
          rm tools.zip
          mkdir -p latest
          mv temp/cmdline-tools/* latest/
          rm -rf temp
          echo "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin" >> $GITHUB_PATH

      - name: ✅ Принимаем лицензии
        run: |
          yes | sdkmanager --sdk_root=$ANDROID_SDK_ROOT --licenses

      - name: 📲 Устанавливаем необходимые Android пакеты
        run: |
          sdkmanager --sdk_root=$ANDROID_SDK_ROOT "platform-tools" "platforms;android-31" "build-tools;36.0.0" "ndk;25.1.8937393"

      - name: 🔨 Собираем APK
        run: |
          source venv/bin/activate
          buildozer android debug

      - name: 📤 Загружаем APK в артефакты
        uses: actions/upload-artifact@v4
        with:
          name: financetop.apk
          path: bin/*.apk
