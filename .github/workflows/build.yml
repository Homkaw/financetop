name: Build APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 🔽 Клонирование репозитория
      uses: actions/checkout@v3

    - name: 🐍 Установка Python и pip
      uses: actions/setup-python@v4
      with:
        python-version: "3.12"

    - name: 🛠️ Установка зависимостей Python
      run: |
        python -m ensurepip --upgrade
        python -m pip install --upgrade pip setuptools wheel
        pip install cython buildozer
        pip install "python-for-android[full]"

    - name: Проверка cython
      run: cython --version


    - name: ⚙️ Установка зависимостей system (Ubuntu)
      run: |
        sudo apt update
        sudo apt install -y zip unzip openjdk-17-jdk adb pkg-config libgl1-mesa-dev libgles2-mesa-dev libffi-dev libssl-dev ant

    - name: 📥 Клонирование python-for-android
      run: |
        mkdir -p .buildozer/android/platform
        git clone https://github.com/kivy/python-for-android.git .buildozer/android/platform/python-for-android

    - name: 📦 Сборка APK
      run: |
        buildozer android clean
        buildozer android debug

    - name: 🗃️ Копирование APK в артефакты
      uses: actions/upload-artifact@v4
      with:
        name: Financetop-APK
        path: bin/*.apk
