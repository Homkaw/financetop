name: Build APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      ANDROID_SDK_ROOT: /home/runner/android-sdk
      PATH: ~/.local/bin:/home/runner/android-sdk/platform-tools:/home/runner/android-sdk/cmdline-tools/latest/bin:/home/runner/android-sdk/build-tools/33.0.2:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

    steps:
    - name: 🔽 Клонирование репозитория
      uses: actions/checkout@v3

    - name: 🐍 Установка Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.12"

    - name: ⚙️ Установка системных зависимостей (Ubuntu)
      run: |
        sudo apt update
        sudo apt install -y zip unzip openjdk-11-jdk adb pkg-config libgl1-mesa-dev \
          libgles2-mesa-dev libffi-dev libssl-dev ant wget autoconf automake libtool libtool-bin m4 build-essential

    - name: 🛠️ Установка Python-зависимостей
      run: |
        export M4=m4
        python -m ensurepip --upgrade
        python -m pip install --upgrade pip setuptools wheel
        pip install cython buildozer "python-for-android[full]"

    - name: ⚙️ Установка Android SDK и Build-Tools
      run: |
        mkdir -p $ANDROID_SDK_ROOT/cmdline-tools
        cd $ANDROID_SDK_ROOT/cmdline-tools
        wget https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
        unzip -q commandlinetools-linux-9477386_latest.zip -d latest
        mv latest/cmdline-tools/* latest/
        rm -r latest/cmdline-tools
        ln -s $ANDROID_SDK_ROOT/cmdline-tools/latest $ANDROID_SDK_ROOT/tools
        yes | $ANDROID_SDK_ROOT/tools/bin/sdkmanager --sdk_root=$ANDROID_SDK_ROOT --licenses
        $ANDROID_SDK_ROOT/tools/bin/sdkmanager --sdk_root=$ANDROID_SDK_ROOT "platform-tools" "platforms;android-33" "build-tools;33.0.2"

    - name: 📥 Клонирование python-for-android
      run: |
        mkdir -p .buildozer/android/platform
        git clone https://github.com/kivy/python-for-android.git .buildozer/android/platform/python-for-android

    - name: 📦 Установка Apache Ant
      run: |
        wget https://archive.apache.org/dist/ant/binaries/apache-ant-1.9.4-bin.tar.gz
        tar -xzf apache-ant-1.9.4-bin.tar.gz
        sudo mv apache-ant-1.9.4 /opt/ant
        echo "ANT_HOME=/opt/ant" >> $GITHUB_ENV
        echo "/opt/ant/bin" >> $GITHUB_PATH

    - name: ⏳ Инициализация сборки (чтобы появился libffi)
      run: |
        export M4=m4
        timeout 60s buildozer android debug || echo "Initial libffi extraction complete."

    - name: 🧩 Патч libffi для автореконфигурации
      run: |
        LIBFFI_PATH=$(find .buildozer -type d -name "libffi-*" | head -n 1)
        if [ -d "$LIBFFI_PATH" ]; then
          echo "📂 Найден путь: $LIBFFI_PATH"
          cd "$LIBFFI_PATH"
          sed -i '/AC_INIT/ a m4_pattern_allow([^LT_SYS_SYMBOL_USCORE])' configure.ac
          libtoolize --force --copy
          aclocal
          autoconf
          automake --add-missing
          autoreconf -fi
        else
          echo "⚠️ Папка libffi не найдена — патч пропущен."
        fi



    - name: 🧼 Очистка и финальная сборка APK
      run: |
        export M4=m4
        buildozer android clean
        buildozer android debug --verbose

    - name: 📦 Загрузка APK как артефакт
      uses: actions/upload-artifact@v4
      with:
        name: Financetop-APK
        path: bin/*.apk
